# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# Example for Windows local PostgreSQL:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/gradcafe_db

# Flask Configuration
# Port to run the Flask app (default: 5000)
PORT=5000

# Host address - defaults to 0.0.0.0 (accessible from network)
# For local-only access on Windows, uncomment and use:
# HOST=127.0.0.1
HOST=0.0.0.0

# Enable debug mode (True/False)
FLASK_DEBUG=False

# Secret key for Flask sessions (change this in production!)
SECRET_KEY=your-secret-key-change-in-production

# LLM Configuration (Optional - only needed if using the LLM standardization service)
# MODEL_REPO=TheBloke/TinyLlama-1.1B-Chat-v1.0-GGUF
# MODEL_FILE=tinyllama-1.1b-chat-v1.0.Q4_K_M.gguf
# N_THREADS=2
# N_CTX=2048
# N_GPU_LAYERS=0
